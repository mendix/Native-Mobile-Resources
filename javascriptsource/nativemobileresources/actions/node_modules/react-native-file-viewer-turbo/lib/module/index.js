"use strict";

import FileViewerTurbo from "./NativeFileViewerTurbo.js";
let dismissListener = null;
export async function open(path, options = {}) {
  const {
    onDismiss,
    ...nativeOptions
  } = options;
  dismissListener = FileViewerTurbo.onViewerDidDismiss(() => {
    onDismiss?.();
    dismissListener?.remove();
  });
  await FileViewerTurbo.open(normalize(path), nativeOptions);
}
function normalize(path) {
  const filePrefix = 'file://';
  if (path.startsWith(filePrefix)) {
    path = path.substring(filePrefix.length);
    try {
      path = decodeURI(path);
    } catch {
      // ignore decode errors
    }
  }
  return path;
}
//# sourceMappingURL=index.js.map