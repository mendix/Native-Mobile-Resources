"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.open = open;
var _NativeFileViewerTurbo = _interopRequireDefault(require("./NativeFileViewerTurbo"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
let dismissListener = null;
async function open(path, options = {}) {
  const {
    onDismiss,
    ...nativeOptions
  } = options;
  dismissListener = _NativeFileViewerTurbo.default.onViewerDidDismiss(() => {
    onDismiss?.();
    dismissListener?.remove();
  });
  await _NativeFileViewerTurbo.default.open(normalize(path), nativeOptions);
}
function normalize(path) {
  const filePrefix = 'file://';
  if (path.startsWith(filePrefix)) {
    path = path.substring(filePrefix.length);
    try {
      path = decodeURI(path);
    } catch {
      // ignore decode errors
    }
  }
  return path;
}
//# sourceMappingURL=index.js.map