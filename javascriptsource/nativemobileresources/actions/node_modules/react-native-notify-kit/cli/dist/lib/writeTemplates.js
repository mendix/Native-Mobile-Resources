"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeTemplates = writeTemplates;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const initNseCore_1 = require("./initNseCore");
/**
 * Writes the NSE template files (Swift, Info.plist, entitlements) to the
 * target directory under the iOS project.
 */
function writeTemplates(opts) {
    const targetDir = path.join(opts.iosDir, opts.targetName);
    if (fs.existsSync(targetDir) && !opts.force) {
        throw new Error(`NSE target '${opts.targetName}' appears to exist at ${targetDir}.\n` +
            '  Use --force to overwrite or --target-name to use a different name.');
    }
    fs.mkdirSync(targetDir, { recursive: true });
    const files = [
        {
            output: 'NotificationService.swift',
            content: (0, initNseCore_1.renderNotificationServiceSwift)(),
        },
        {
            output: 'Info.plist',
            content: (0, initNseCore_1.renderNseInfoPlist)({ targetName: opts.targetName }),
        },
        {
            output: `${opts.targetName}.entitlements`,
            content: (0, initNseCore_1.renderNseEntitlementsPlist)(),
        },
    ];
    const written = [];
    for (const { output, content } of files) {
        const outputPath = path.join(targetDir, output);
        fs.writeFileSync(outputPath, content, 'utf-8');
        written.push(outputPath);
    }
    return written;
}
//# sourceMappingURL=writeTemplates.js.map