import ReactNativeBiometrics from 'react-native-biometrics';

// This file was generated by Mendix Studio Pro.
// BEGIN EXTRA CODE
// END EXTRA CODE
/**
 * @param {string} reason - The reason for requesting authentication. If empty, the system default message is used.
 * @returns {Promise.<boolean>}
 */
async function BiometricAuthentication(reason) {
    // BEGIN USER CODE
    // Documentation https://github.com/smallcase/react-native-simple-biometrics
    const rnBiometrics = new ReactNativeBiometrics();
    return rnBiometrics
        .simplePrompt({ promptMessage: reason !== null && reason !== void 0 ? reason : "" })
        .then(() => true)
        .catch(() => false);
    // END USER CODE
}

export { BiometricAuthentication };
